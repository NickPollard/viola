**Viola**

A CoffeeScript monadic collections library inspired by and largely based upon the Scala Collections library.

Currently implements the following types:

* Option
* List
* Map (using D3)
* Tuples (Pairs/Triples/Quads)

Also adds useful methods to Javascript Arrays.
